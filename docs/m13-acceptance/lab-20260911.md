# M01/M13: Unified Image Deployment on the Lab PC

> **Code style requirement: Economical code, exceptional readability, and excellent abstraction design.**

Status: software deployment accepted; robot/camera acceptance not run, 2026-09-11.

The user confirmed lab connectivity, authorized deletion of the eight named early
local image archives while retaining `ur12e-simulator-6e48d82`, and requested
synchronization of the current image to `ssh ur12e-collection`. The UR12e remains
offline. Do not contact the robot, start control, poll Hand-E, or open cameras.
This visit covers software deployment only, not hardware acceptance.

## Scope and procedure

1. Delete only the eight `image.tar` files listed in M01 image-consolidation.md.
   Preserve historical manifests/reports and mark those directories incomplete.
   Keep current and rollback archives; do not prune Docker images or caches.
2. Inspect the collection PC and preserve the existing station config hash.
   Transfer the immutable `ur12e-unified-bbc56a8` bundle to a new staging
   directory, verify every checksum, then publish it without overwriting old data.
3. Run the actual loader, verify image identity/source hashes and dependencies,
   non-root network-isolated diagnostics, and installed software tests. Use
   disposable smoke mounts, then verify existing production mount permissions
   without network or device access. Do not modify station.json or old recordings.
4. Set an explicit current-bundle symlink after successful validation. Record
   software PASS/FAIL results separately from robot/camera cases, which remain
   NOT RUN. Preserve failed evidence and do not deploy a failed artifact.

M01-A01/A02: exact archive/image identity, source-free startup and installed
software tests. M01-A03: writable mount behavior and unchanged production config.
M13-A04: no physical acceptance inferred from software-only deployment.


## Deployed artifact and results

Host `ur12e-flexlab` is Ubuntu 24.04.3 LTS, x86_64, Docker 29.4.0 / Compose 5.1.2;
operator `robot2026fall` uses UID/GID 1011:1011. Host pytest is 7.4.4;
the installed container uses pinned Python 3.12/Jazzy dependencies.

Bundle: `/home/robot2026fall/ur12e-unified-bbc56a8`.
Current selector: `/home/robot2026fall/ur12e-current` (symlink to that bundle).
Image: `ur12e-collection:current`, immutable ID
`sha256:a3d22d1ffa6c36331116a4c870f7bb841d84ec036cd9a2d23460345ce15bab96`.
Source: `bbc56a8c714e7a8bc1b6f9124af80339352a2b97`.
Archive: 485,872,640 bytes; SHA-256
`b25853739e43ec35af0a7ec4cfafc653c83b8d7d924da8d71773a5f5847b0121`.

| Gate | Result | Evidence |
| --- | --- | --- |
| M01-A01/A02 transfer/load | PASS | All 17 file checksums before publication and after tests; actual bundle loader imported the exact image |
| M01-A02 installed software | PASS | 255 tests pass natively on Ubuntu; two host-only mount tests skipped here and run separately |
| M01-A02 source-free launch | PASS | Bundled dev profile overrides a deliberately invalid environment image with the manifest ID; doctor succeeds with no network/device access |
| M01-A03 persistence/mounts | PASS | Both host mount tests pass; existing production directories are writable and station structure validates |
| M01-A01 identity | PASS | All 59 package hashes match the delivered manifest; non-root amd64 image/source label matches |
| M02 config preservation | PASS | Existing station.json SHA-256 unchanged before/after: `17c3b9e2e505972f90a2dc0add6ea64f23cdd830dda1154f60c015b24424bb9c` |
| M13-A04 physical distinction | PASS | All verification containers use `--network none`, no devices; robot, Hand-E and cameras are not opened |
| Fresh-machine / physical / motion | NOT RUN | Existing lab PC; UR12e offline; no collection or motion started |

The first SSH-streamed verification shell ended after Compose consumed its stdin.
Verification resumed from an uploaded script file with stdin closed, then all
checks above completed. This affected orchestration only; no image/config change
or relaxed acceptance criterion was used. The current selector was created only
after all software and host-mount tests passed.

Remote evidence: `/home/robot2026fall/ur12e-deployment-20260911/`.
Local copy: `artifacts/lab-20260911/remote-evidence/`; transfer/orchestration and
explicit eight-archive cleanup records are beside it. Eight early local
`image.tar` files were removed (3,770,061,824 bytes); current and rollback archives
are retained. Docker tags, remote historical bundles, volumes and recordings
were not deleted.

Use `/home/robot2026fall/ur12e-current` for subsequent collection-PC commands.
The robot remains offline and physical control remains disabled. Reconnect and
review device availability with the user before any new hardware acceptance;
software deployment does not authorize real motion.
