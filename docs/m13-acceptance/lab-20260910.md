# M01 / M13 Lab Deployment: 2026-09-10

> **Code style requirement: Economical code, exceptional readability, and excellent abstraction design.**

Status: deployment and software validation passed; physical acceptance pending.
The user authorized image synchronization and camera-free checks on the reachable
lab PC. No robot connection or joint action was performed.

## Delivered artifact and environment

- SSH: `ur12e-collection`; hostname `ur12e-flexlab`.
- Ubuntu 24.04.3 LTS, kernel 6.17.0-1032-oem, x86_64; Intel Core Ultra 9 285,
  24 CPUs and approximately 62 GiB usable RAM.
- Docker 29.4.0, Compose v5.1.2. Containers run as operator UID/GID 1011:1011.
- Bundle: `/home/robot2026fall/ur12e-shadow-bundle-20260910` on the station;
  local copy: `artifacts/releases/ur12e-shadow-bundle-20260910/`.
- Runtime: `sha256:8c393dd43cb8aaec6986489c0990a48aaf3f4eed43673b549b869de67563cbd2`.
  Source label: `15c9d26-working-shadow`, an uncommitted working increment.
- Archive: 474,395,648 bytes, SHA-256
  `75090bc74e191508eb15bd656fe806cc6cd526ef9564259ad4267f216a23ca75`.
  All delivered file checksums passed before load and again after validation.
- Runtime versions match the [M13 plan](plan.md): ROS 2 Jazzy, Python 3.12.3,
  RealSense 2.56.5.9235, ur-rtde 1.6.5, PyAV 15.1.0, OpenCV 4.12.0.88,
  MCAP 1.4.0 and mcap-ros2-support 0.5.7.

This bundle reuses the tested image archive. It corrects the loader's printed
command to include the `station` Compose profile and the camera launcher's
configuration default to `/var/lib/ur12e-collection`. The runbook has the same
corrections. Previous bundles remain unchanged. No sudo was needed.

Actual configuration is `/var/lib/ur12e-collection/station.json`; data is under
`/var/lib/ur12e-collection/data`. The configuration SHA-256 remained
`17c3b9e2e505972f90a2dc0add6ea64f23cdd830dda1154f60c015b24424bb9c`.
Synthetic recordings use separate output directories and synthetic snapshots.

## Results

| Acceptance case | Result | Observation |
| --- | --- | --- |
| M01-A01.1 / M01-A02.2 | PASS | Bundle loaded by immutable image ID; hardware-backend doctor imports every selected dependency; operator mounts writable; station structure valid |
| M01-A02.1-smoke | PASS | Source-free bundle runs on the existing lab PC, with no registry pull |
| M01-A02.1 clean machine | NOT RUN | Existing host is not a fresh Ubuntu installation |
| M01-A03.1 / M01-A03.2 | PASS | Both existing container persistence/read-only tests passed on the station with host pytest 7.4.4; isolated fixtures preserved |
| M07-A01/A02/A03 | PASS synthetic only | Three persistent 640x480 / 30 Hz sources; no source counter gaps or repeated depth frames across the successful batches |
| M09-A04 / M13-A01/A02 | PASS synthetic only | 2 x 2 s smoke, 2 x 40 s duration, and 20 x 2 s lifecycle batches completed |
| M10-A03 / M11-A04 | PASS software | All 24 completed episodes independently reverified from read-only mounts; all RGB frames decoded and all depth hashes verified; snapshots explicitly simulated |
| M11-A04 | PASS ROS software | Native ROS reader deserialized 414 messages across eight topics from the first smoke episode; bag info and 10x playback exited successfully |
| M09-A04 / M13-A04 | PASS synthetic interrupt | SIGINT sent after an active MCAP partial appeared; exit 130, interrupted report, zero completed episodes, partial preserved, no cleanup errors or remaining containers |
| M13 startup prerequisites | PASS rejection behavior | Camera inventory is empty; hardware shadow exits 1 because the station lacks three serial bindings; no simulated fallback |
| M13-A03 physical 20 x 40 s | NOT RUN | Cameras are not connected/bound |

All synthetic collection and independent verification containers used
`--network none`. ROS playback was isolated in its own network namespace;
this validates transport availability, not GUI rendering or scene quality.
The runtime's full unit suite was already tested before packaging; this visit
ran deployment/integration checks and the two host mount tests, not a new full
unit-suite run.

| Synthetic batch | Completed | Accepted groups | MCAP bytes | Peak writer queue | Maximum queue delay |
| --- | ---: | ---: | ---: | ---: | ---: |
| Smoke, 2 x 2 s | 2 | 118 | 2,533,138 | 1 | 0.457 ms |
| Duration, 2 x 40 s | 2 | 2,398 | 51,245,110 | 1 | 0.467 ms |
| Lifecycle, 20 x 2 s | 20 | 1,187 | 25,486,505 | 2 | 13.232 ms |

The duration episodes each accepted 1,199 groups and recorded one excessive-skew
rejection. Source receipt rates were approximately 30 Hz. No queue overflow
occurred; queue and codec settings were unchanged. One full-container sample
during duration capture measured 61.75% Docker CPU (approximately 0.62 CPU core)
and 257.1 MiB memory. This is a sample, not a measured peak. Synthetic scene
entropy and absent USB acquisition prevent extrapolating these costs to cameras.

## Evidence and next action

Remote logs/scripts: `ur12e-shadow-bundle-20260910/evidence/`. Completed data and
reports: `/var/lib/ur12e-collection/data/lab-20260910-{smoke-2x2,duration-2x40,lifecycle-20x2}`;
interrupt evidence is `lab-20260910-interrupt`. Local reports/logs and execution
scripts are under ignored `artifacts/lab-20260910/`. No recordings were deleted.

After connecting cameras, enter the delivered bundle and run:

```sh
./scripts/camera-probe
# Identify physical wrist/left/right roles and edit the actual station.json.
./scripts/run station station validate /config/station.json --cameras-ready
./scripts/camera-shadow --station /config/station.json \
  --revision 15c9d26-working-shadow --output /data/physical-shadow-smoke-20260910 \
  --episodes 2 --seconds 2
```

Then follow the [lab runbook](lab-runbook.md) for 20 x 40 seconds, USB/frame/clock
measurements, image quality, and live interrupt/disconnect checks. Physical
clock accuracy, actual compression cost, SDK cleanup, and camera throughput
remain pending. ZERO/READY, GELLO, Hand-E and keyboard leading remain separate
module work. No robot motion is part of this acceptance batch.
