# Lab Read-Only Load and 120 Hz Integration

**Code style requirement: Economical code, exceptional readability, and excellent abstraction design.**

Status: implementing the aligned M04 and read-only load scope; M06 control-rate
production changes remain proposed pending the concrete simulator result.

## Alignment and boundaries

On 2026-09-12 the user selected 120 Hz leader acquisition, accepted integrating
the proven Fast Sync Read / fixed-period prototype and requested full collection
stress testing. The existing M04 record defines the prototype and unchanged
freshness/queue behavior. The user additionally requested UR control at 60 Hz,
preferably 120 Hz. Physical UR remains Manual and physical control is forbidden.
This request does not authorize motor torque, goals, UR programs or motion.

## Ordered work

1. M04: integrate 120 Hz Fast Sync Read into the persistent reader. Verify the
   observed firmware supports the protocol before using it. Schedule against
   fixed monotonic deadlines, fit the independent 1 Hz health reads within idle
   time, skip missed slots and preserve actual acquisition times. Keep existing
   100 ms freshness, source fault latching and bounded queues. Add deterministic
   schedule, health-work, overrun and unsupported-firmware regression cases.
2. M07/M08/M11/M13: run one short real-camera smoke, then a fresh 20 x 40-second
   camera recording batch concurrently with one continuous physical 120 Hz
   reader. Exercise actual RGB/depth alignment, matching, H.264, lossless depth,
   final-file verification and disk I/O. Keep source provenance and raw leader
   evidence in separate explicit test artifacts; do not claim a control-session
   or leader-in-MCAP semantic gate from an independent load test. No UR writes.
   Preserve the existing tested camera configuration and all thresholds.
3. M06 proposed simulator experiment: compare 60/120 Hz using a single intended
   command period across scheduler, SDK frequency, servo duration and snapshot.
   Inspect the controller's actual accepted/observed cadence rather than infer it
   from the requested number. Retain all speed/acceleration/step/freshness bounds,
   watchdog and stop policy. Frequency is not permission to increase speed.
   Validate asynchronous leader arrival, branch/reversal paths, stop and stalled
   controller ownership in verified isolated URSim. Do not enable physical control.
4. Record evidence and remaining work. A failed full batch stays FAIL; do not
   combine episodes from different attempts or relax quality/freshness gates.
   Source-overlay tests are development evidence, not installed-image acceptance.
   Build/redeploy only after the changed software tests pass.

## Acceptance

| Slice | Required evidence | Initial status |
| --- | --- | --- |
| M04-A01/A03 | 120 Hz schedule, verified fast-read firmware, unchanged fault/queue behavior; raw seven-axis trace and zero write opcodes | Isolated prototype PASS; production integration pending |
| M08/M11/M13 read-only load | Fresh 20 x 40 batch, independent complete-file audit, camera grouping/skew, leader gaps/ages/CPU, writer/rig queue and storage costs | NOT RUN |
| M06-A01/A03/A04 | Actual URSim 60/120 Hz, limits and stop/fault audit, truthful rate metadata | NOT RUN; physical control excluded |

Test artifacts stay under ignored `artifacts/` and dedicated station output
directories. Existing data/configuration and the accepted current image remain
unchanged. Do not install persistent USB latency rules or alter CPU affinities
without evidence that those changes are needed.

## M04 implementation checks

The persistent worker now uses Fast Sync Read after firmware validation and a
120 Hz fixed-phase schedule, with health work inside the idle budget and missed
slots skipped. The seven added cases cover expired deadlines, phase preservation,
unsupported firmware before fast reads, and deterministic health work plus sleep
overshoot. Native full regression PASS: 438 tests / five environment skips
(10.85 s); focused reader/bus/episode/input cases PASS (41). Format and
production/script Pylint pass after correcting an overlong docstring. Physical
combined-load and installed-image checks follow; no test changes motion bounds.
