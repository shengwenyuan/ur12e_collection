# Lab Read-Only Load and 120 Hz Integration

**Code style requirement: Economical code, exceptional readability, and excellent abstraction design.**

Status: implementing the aligned M04 and read-only load scope; M06 control-rate
production changes remain proposed pending the concrete simulator result.

## Alignment and boundaries

On 2026-09-12 the user selected 120 Hz leader acquisition, accepted integrating
the proven Fast Sync Read / fixed-period prototype and requested full collection
stress testing. The existing M04 record defines the prototype and unchanged
freshness/queue behavior. The user additionally requested UR control at 60 Hz,
preferably 120 Hz. Physical UR remains Manual and physical control is forbidden.
This request does not authorize motor torque, goals, UR programs or motion.

## Ordered work

1. M04: integrate 120 Hz Fast Sync Read into the persistent reader. Verify the
   observed firmware supports the protocol before using it. Schedule against
   fixed monotonic deadlines, fit the independent 1 Hz health reads within idle
   time, skip missed slots and preserve actual acquisition times. Keep existing
   100 ms freshness, source fault latching and bounded queues. Add deterministic
   schedule, health-work, overrun and unsupported-firmware regression cases.
2. M07/M08/M11/M13: run one short real-camera smoke, then a fresh 20 x 40-second
   camera recording batch concurrently with one continuous physical 120 Hz
   reader. Exercise actual RGB/depth alignment, matching, H.264, lossless depth,
   final-file verification and disk I/O. Keep source provenance and raw leader
   evidence in separate explicit test artifacts; do not claim a control-session
   or leader-in-MCAP semantic gate from an independent load test. No UR writes.
   Preserve the existing tested camera configuration and all thresholds.
3. M06 proposed simulator experiment: compare 60/120 Hz using a single intended
   command period across scheduler, SDK frequency, servo duration and snapshot.
   Inspect the controller's actual accepted/observed cadence rather than infer it
   from the requested number. Retain all speed/acceleration/step/freshness bounds,
   watchdog and stop policy. Frequency is not permission to increase speed.
   Validate asynchronous leader arrival, branch/reversal paths, stop and stalled
   controller ownership in verified isolated URSim. Do not enable physical control.
4. Record evidence and remaining work. A failed full batch stays FAIL; do not
   combine episodes from different attempts or relax quality/freshness gates.
   Source-overlay tests are development evidence, not installed-image acceptance.
   Build/redeploy only after the changed software tests pass.

## Acceptance

| Slice | Required evidence | Initial status |
| --- | --- | --- |
| M04-A01/A03 | 120 Hz schedule, verified fast-read firmware, unchanged fault/queue behavior; raw seven-axis trace and zero write opcodes | Isolated prototype PASS; production integration pending |
| M08/M11/M13 read-only load | Fresh 20 x 40 batch, independent complete-file audit, camera grouping/skew, leader gaps/ages/CPU, writer/rig queue and storage costs | NOT RUN |
| M06-A01/A03/A04 | Actual URSim 60/120 Hz, limits and stop/fault audit, truthful rate metadata | NOT RUN; physical control excluded |

Test artifacts stay under ignored `artifacts/` and dedicated station output
directories. Existing data/configuration and the accepted current image remain
unchanged. Do not install persistent USB latency rules or alter CPU affinities
without evidence that those changes are needed.

## M04 implementation checks

The persistent worker now uses Fast Sync Read after firmware validation and a
120 Hz fixed-phase schedule, with health work inside the idle budget and missed
slots skipped. The seven added cases cover expired deadlines, phase preservation,
unsupported firmware before fast reads, and deterministic health work plus sleep
overshoot. Native full regression PASS: 438 tests / five environment skips
(10.85 s); focused reader/bus/episode/input cases PASS (41). Format and
production/script Pylint pass after correcting an overlong docstring. Physical
combined-load and installed-image checks follow; no test changes motion bounds.

## Initial deployed checks and USB decision

Commit `7028962` built on the lab PC as `ur12e-collection:load-7028962`.
Installed regression PASS: 441 tests / two host-only skips (14.30 s); one pytest
cache permission warning does not affect results. The short concurrent smoke
completed one 10-second camera file: 298/300 anchors accepted, all RGB decoded
and all depth hashes verified. Three camera sources each delivered approximately
30 fps without color/depth gaps; writer queue peak 1/4. This 99.33% short boundary
result is not promoted to a complete batch-quality pass.

Current physical topology is D405 at 5 Gbps/USB 3.2, D435IF 327122073926 directly
on a 480 Mbps port, and D435IF 327122075735 through a 480 Mbps hub. On 2026-09-12
the user confirmed the cabling cannot currently be replaced and explicitly chose
to run the full batch as connected. Do not attribute later failures to USB alone
without evidence or downgrade the agreed stream/gate settings.

## URSim rate exploration

A disposable verified-URSim test varied host/SDK command frequency and servo
period together while preserving the 125 Hz independent readback recipe, all
motion limits and watchdog behavior. Its first launch used the wrong frozen
fixture path and exited before opening a simulator connection; the corrected
launch completed both cases. Each used a 20-second asymmetric wave and a measured
stop followed by three seconds of hold observation:

| Requested host command rate | Actual send rate | Gap p99 / max | Stop | Hold drift |
| --- | --- | --- | --- | --- |
| 60 Hz | 60.019 Hz | 17.415 / 21.397 ms | 0.314 s | 0 rad observed |
| 120 Hz | 120.000 Hz | 9.857 / 10.917 ms | 0.301 s | 0 rad observed |

Evidence: `artifacts/simulator-control/rates-1789152283192415219` and the local
`ursim-rates-recheck.log`. These are application send rates, not a claim that the
robot applied every packet at exactly that frequency. The dedicated receipt and
applied-command audit, shared-session recording/fault gates and production
configuration still need alignment and validation. No real UR connection was
opened. UR's [official publisher documentation](https://docs.universal-robots.com/Universal_Robots_ROS_Documentation/rolling/doc/ur_rtde_ros2_publisher/doc/usage.html)
requires output frequency to align to the 500 Hz update cycle; retaining 125 Hz
readback keeps that separate from the chosen 120 Hz host target cadence.

## M06 concrete implementation alignment

The user explicitly selected 120 Hz host target sending with independent 125 Hz
RTDE feedback on 2026-09-12, retaining limits and no physical control. Implement
one shared command-rate constant for simulator SDK construction, servo duration,
interactive/session test pacing and snapshot metadata. Use the verified fixed-phase
schedule without catch-up bursts. Keep old 50 Hz snapshots valid; accepting an
old archive must never silently relabel its rate. Feedback remains independently
timestamped at 125 Hz. No interpolation or extra motor/robot control entrypoint
is introduced.

Acceptance: deterministic pacing and historical/new snapshot compatibility;
existing control limit/fault tests; actual URSim shared-session capture with
replayed leader input, independently audited command rate and stop behavior;
existing complex motion and session-fault tests at the selected command period.
Do not infer physical 120 Hz acceptance from these simulator results. Read-only
lab pressure continues independently in the already frozen 7028962 image.

## Shared 120 Hz control checks

Production cadence now shares the 120 Hz constant across SDK setup, servo period
and snapshot; fixed-phase console pacing skips expired slots. The 125 Hz feedback
rate is separately recorded in new snapshots. Historical 50 Hz snapshots remain
valid and keep their original value. The accepted 60 Hz comparison is also a
valid explicit snapshot rate. Camera cadence and all motion limits are unchanged.

Native regression after the control change PASS: 443 tests / five environment
skips (10.67 s). Focused snapshot/console/leader gates PASS after adding explicit
feedback metadata; format and production/script Pylint PASS. Six additional
independent-load-auditor tests reject hidden leader sequence/epoch/gap/health/
torque faults even when the camera summary claims success.

Actual URSim shared-session replay PASS: two 10-second episodes with recorded
leader input, files independently decoded/verified, stop/held and next-episode
handover. Recorded command rates were 119.387 and 118.803 Hz, with truthful 120 Hz
requested metadata; this is not an exact-rate claim under camera workload.
Evidence: `session-1789152562155826959`. The short synthetic-camera boundaries
include rejected groups and are not a fresh 20 x 40 camera-quality pass. The
scaled control-rate audit retains the old 90% minimum ratio (45/50) against the
recorded requested rate instead of allowing a stale hard-coded 45 Hz threshold.

Actual complex motion PASS: `motion-1789152637664506008.json`, six asymmetric,
branch and near-bound poses; SDK READY peak 0.3 rad/s; move/servo stops 0.709/
0.279 s with zero observed hold drift. Active discard, recorder loss and actual
SIGINT PASS at the new session cadence, with shared memory released:
`session-faults-1789152710929042542`. These are simulation-only results.

The first additional eight-case campaign did not complete: six targeted fault
paths plus the range-labelled stop were reached, but the final held-torque case
failed during its pre-fault recording phase on a recording-command queue overflow.
This is a retained Mac/amd64 integrated-load failure, not a passed held-torque
injection. Automatic stop occurred; no queue or timing gate was enlarged.

Higher command cadence also exposed a fixture weakness: range injection could
mutate a cached sample and trip chronology before testing calibrated bounds.
The fixture now waits for a fresh acquisition and asserts the calibrated-range
error; a deterministic regression covers this distinction. Its replay-speed
argument now actually reaches Trace, and focused range/held-torque selection is
available for diagnostics. The original full campaign used the fixture's prior
1x behavior despite a 3.5 CLI argument; the fresh recheck explicitly requests 1x.
Neither fixture correction changes production control behavior. The complete
recheck and any remaining failure are recorded below before delivery.

The corrected complete eight-case campaign PASSes at 120 Hz:
`leader-faults-1789152973173228928`. The range case now reports the actual
calibrated-interval rejection. Held-torque injection faults from held review and
retains its one previously completed episode. HOME recorder death, stale source,
epoch change, recorder death, injected disk full and injected backlog also PASS.
No limits, queues or deadlines were enlarged. The first campaign's pre-injection
queue overflow remains an unexplained intermittent Mac/amd64 reliability result;
a later pass does not establish its root cause or turn that run into a pass.

Final native regression PASS: 450 tests / five environment skips (10.87 s),
Black 159 files PASS, production/script Pylint 10.00 with no findings. The new
image will record the committed source identity. Ubuntu's ongoing 20-episode
load run remains on its original frozen 7028962 image; no build or source update
has changed that process during measurement.
