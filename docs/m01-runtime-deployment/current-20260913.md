# M01: Current Collector Consolidation, 2026-09-13

**Code style requirement: Economical code, exceptional readability, and excellent abstraction design.**

Status: accepted for offline deployment. The operator requested committing the latest validated
changes, consolidating Mac and PC collector images, and archiving obsolete PC
deployment paths under `~/past_archives`.

## Scope and acceptance

1. Commit the 2-degree tracking threshold, its regression and recorded physical
   acceptance. Keep unresolved stop-timeout/watchdog handling visible in M09.
2. Default the teleop launcher to `ur12e-collection:current`. Reuse installed
   dependencies without downloads, refreshing release configuration, launchers
   and tests. Preserve one current collector image identity on both hosts;
   compatibility selectors may reference this same image.
3. M01-A01: match image identity, installed package hashes and configuration on
   Mac and PC. M01-A02: pass offline software checks without hardware access.
4. M01-A03: point `~/ur12e-current` to the active deployment; archive superseded
   collector directories and associated old release reports. Preserve local
   station configuration, recordings, `~/ur12e-data`, external Isaac scene/assets
   and unrelated training images. Check external path references before moves.
5. Remove obsolete collector image tags after validation. Do not prune unrelated
   images, volumes, build caches or active containers. Record actual results.

No hardware controls or new motion acceptance are part of this delivery.

## Results

- M01-A01 PASS: source commit `8b2ebff`; both hosts select image
  `sha256:d3e5186685f3efd37295e0aa71a1b2969b0e7b8b19801cb63107445280787629`.
  All 108 installed package hashes match committed source. Runtime remains
  Ubuntu 24.04 / ROS 2 Jazzy / Python 3.12, linux/amd64, non-root collector.
  Existing image layers were reused with networking disabled; no dependencies
  were downloaded. Updated configuration, launchers and tests are in `/opt`.
- M01-A02 PASS: 621 tests passed, 5 skipped on native Mac, Mac installed image
  and PC installed image. Black checked 193 files; production Pylint passed at
  10/10. Initial sandbox run had five process/shared-memory permission failures;
  authorized native rerun passed. Docker build initially lacked buildx cache
  metadata permission; authorized retry passed. No hardware tests were enabled.
- M01-A03 PASS: image archive SHA-256
  `bf089da155a6b06d9f072c3d8e634aaf3e541c091e2c4098998340f6be074d11`
  matched after transfer. Active PC deployment is `~/ur12e-real-teleop`, selected
  by `~/ur12e-current`. The physical configuration matches local SHA-256
  `1b32175b5209fc251c65439ac3ba7ac48ac3242492af1f8d198e0c13bf12a67a`.

Both hosts retain one collector image identity. Tags `current`, `physical-teleop`
and `native-isaac` are aliases of it, preserving existing commands. All other
collector tags were removed; unrelated images, URSim and build caches remain.

PC archive: `~/past_archives/consolidation-20260913` contains the former
`ur12e-collection-sim`, `ur12e-physics-dev`, `ur12e-unified-5185ee5` directories,
four old unified-release reports, and the previous active-source/manifest backup.
No external runtime/config reference to those old deployments was found in the
checked Isaac scripts, active collector configuration or user service/bin paths.
Independent `~/ur12e-sim`, `~/ur12e-models`, `~/ur12e-data`, original recordings
and local station settings remain in place. Archive path inventory, build input,
image archive and test logs remain available for audit.

The launcher defaults to `current`; existing explicit `physical-teleop` commands
still select the same image. This release includes the observed physical episode
successes and unresolved stop-timeout/watchdog issue documented in M09. It does
not claim that the latter issue was repaired by image consolidation.
