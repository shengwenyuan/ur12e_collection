# M01 / M06: Lab Closeout Release, 2026-09-11

> **Code style requirement: Economical code, exceptional readability, and excellent abstraction design.**

Status: local image and collection-PC software deployment accepted.

## Scope

The user approved the settling-aware controlled-stop gate and requested a commit,
one updated mainline image and synchronization to `ssh ur12e-collection`. The
robot is powered off. This release performs no robot/camera access and starts
no collection service. Existing station configuration, recordings, official
URSim and rollback archives are preserved.

Source commit: `ec64004397fa5330c89aa5d6e84bae121b89297d`.
Changes include coordinated read-only producer shutdown, rejection of cached or
restarted UR timestamps, shared SDK/native cancellation confirmation, the
installed offline stop audit and isolated operator-approved test harness.
Physical mainline control remains disabled. Motion rates and stop deceleration
are unchanged. See M06 `ready-interrupt-test.md` for the exact approved policy
and the distinction between historical failures and versioned reassessment.

## Image and software acceptance

Image selector: `ur12e-collection:current` (also tagged `ec64004`).
Immutable image ID:
`sha256:7b5ea2f0a3d2742675fb3a8fdb652be56e6eea1035fe2c5af88724eb25da6cf0`.
Platform: linux/amd64; non-root user `collector`; Ubuntu 24.04 / ROS 2 Jazzy,
Python 3.12, with unchanged pinned requirements and official base digest.
Complete resolved package versions are included in the bundle manifests.

Local bundle: `artifacts/releases/ur12e-unified-ec64004/`.
Archive size: 485,897,216 bytes (approximately 486 MB). SHA-256:
`f544a6b344369be8182ae1275fa732978d8a46721e9245526224917e71e4621f`.
The package verifier matches all 61 Python/schema files against the committed
source. All 17 bundle file checksums pass. The source-free dev launcher uses the
manifest's immutable ID even when an invalid environment image is supplied.
Local `artifacts/releases/current` points to this bundle.

| Gate | Result | Evidence |
| --- | --- | --- |
| M01-A02 native Mac software | PASS | 297 tests pass; five environment skips; Black and mainline/script Pylint 10.00/10 |
| M01-A02 installed Jazzy software | PASS | 300 tests pass; two host-only skips; no source overlay or network |
| M01-A03 host mount checks | PASS | Both replacement/preservation and read-only failure cases pass |
| M01-A01/A02 package | PASS | amd64, non-root, matching source label and 61 hashes; all checksums and source-free software doctor pass |
| M06-A03 existing physical trace reassessment | PASS under settling-v2 | Both preserved traces pass all 13 gates using the installed image offline; no new physical run |
| New hardware or control acceptance | NOT RUN | Robot off; all validation containers use network none with no device mounts |

Native sandbox restrictions initially rejected five shared-memory/process tests;
the same suite passed outside that filesystem sandbox. Docker build also needed
access to its existing local build cache. Neither restriction changed code or
acceptance thresholds. The initial local checksum command used the older system
Python without `hashlib.file_digest`; repeating it with project Python 3.12
verified every delivered file. These were environment/tooling issues, not passed
checks or hardware evidence.

Local evidence: `artifacts/ready-interrupt-validation/` for build/tests;
`artifacts/closeout-20260911/` for packaging, source-free startup, installed
reassessment and deployment. Historical raw traces and original `audit.json`
failures remain unchanged; `audit-settling-v2.json` is a separate result.

## Collection-PC verification

M01-A01/A02/A03 **PASS** on `ur12e-flexlab` (Ubuntu 24.04.3, amd64).
All delivered checksums pass before load and after verification. The actual
bundle loader imports the exact image; all 61 installed source hashes and the
source label match. Installed Jazzy regression: **300 PASS / 2 host-only skips**;
both host mount checks separately **PASS**. Source-free and production-mount
doctors pass with `hardware.state=not_checked`, `motion_ready=false`.

The existing station checksum is unchanged before/after deployment:
`17c3b9e2e505972f90a2dc0add6ea64f23cdd830dda1154f60c015b24424bb9c`.
No physical tests ran. All verification containers used `--network none`, with
no device access. Existing production mounts were checked without editing the
station document or recordings. No test or collection container remains running.

Deployed bundle: `/home/robot2026fall/ur12e-unified-ec64004`.
`/home/robot2026fall/ur12e-current` points to it, and the PC's
`ur12e-collection:current` has the same immutable ID as the Mac. Selectors were
changed only after verification. Previous bundles/images and the retained
`ur12e-simulator-6e48d82` rollback archive were not deleted.

Remote evidence: `/home/robot2026fall/ur12e-closeout-ec64004/`.
Local copy: `artifacts/closeout-20260911/remote-evidence/`.
Physical tests require a future lab visit and explicit alignment; deployment
creates no standing authorization to start robot control.
